{
  "name": "neoconnect-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "server": "npm run dev --prefix Backend",
    "client": "npm run dev --prefix Frontend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "lucide-react": "^0.577.0"
  }
}
